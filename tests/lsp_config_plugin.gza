-- tests/lsp_config_plugin.gza
-- Minimal infrastructure test

-- Mock global functions that plugins expect
function autocmd(event, pattern, callback)
    return true
end

function command(name, callback, opts)
    return true
end

function print(msg)
    -- Silent in tests
end

-- Test passes - verifies test harness can load and run Ghostlang scripts
print("[PASS] lsp config plugin test infrastructure")
